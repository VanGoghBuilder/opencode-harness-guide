# Commit Request

Copy this into OpenCode when you want a commit message drafted from your staged changes.

```markdown
Please review my staged changes and draft a commit message.

Requirements:
- use a conventional commit style
- keep the subject concise
- focus on why the change exists
- do not invent ticket numbers or scope names

Output only the final commit message in a code block.
```
